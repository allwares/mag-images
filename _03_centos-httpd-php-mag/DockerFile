FROM allwares/centos-httpd-php:latest 
WORKDIR /var/www/html/

COPY --from=allwares/mag-base-zip:latest /magento.zip ./
COPY httpd.conf /etc/https/conf/
COPY php.ini /etc/

RUN unzip ./magento.zip -d ./ \
 		&& rm -f ./magento.zip 


RUN chown -R apache:apache /var/www/html/ \
		&& chmod -R 775 /var/www/html/

